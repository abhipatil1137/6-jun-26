#include <stdio.h>

int main() {
    int page_no = 10;

    printf("\nCurrent Page number is : %d", page_no);

    page_no++;   // Increment page number

    printf("\nPage turn over");
    printf("\nCurrent page number is : %d", page_no);

    int current_emp_id = 100;
    printf("\nNew employee id is : %d", current_emp_id);

    return 0;
}