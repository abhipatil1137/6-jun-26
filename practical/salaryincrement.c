#include <stdio.h>
#include <string.h>

int main(){

    int current_salary = 2000;
    int appaisis_amount = 500;
    int total_salary = current_salary + appaisis_amount;

    printf("Total salary: %d", total_salary);

    return 0;
}